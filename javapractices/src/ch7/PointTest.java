package ch7;

/* class PointTest {
    public static void main(String args[]) {
        Point3D p3 = new Point3D(1,2,3);
    }
}

class Point {
    int x, y;

    Point(int x, int y) {
        this.x = x;
        this.y = y;
    }
    String getLocation() {
        return "x : " + x + ", " + y;
    }
}

class Point3D extends Point {
    int z;

    Point3D(int x, int y, int z) {
        // super(); -> Point() 가 정의되어 있지 않기 때문에 컴파일 오류가 발생한다.
        super(x,y); // 따라서 정의된 생성자를 직접 호출. 
    
        this.z = z;
    }

    String getLocation() {
        return " x : " + x + ", " + y + ", " + z;
    }
}
*/